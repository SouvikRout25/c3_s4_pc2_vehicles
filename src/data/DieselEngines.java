package data;

public class DieselEngines extends ICEngine{
    public DieselEngines(String modelNo, double maxPower, int maxRPM, int displacement, int noOfCylinder, String typeOfFuel) {
        super(modelNo, maxPower, maxRPM, displacement, noOfCylinder, typeOfFuel);
    }
}
